var Outifiter = {
    name: String,
    date: Date,
};

var Product = {
    description: String,
    priceCents: Number,
    priceDecimal: Number,
    image: URL,
    outffiter: Outifiter,
};

//getOpt