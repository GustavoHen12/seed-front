export default class ProductBag {
    constructor(product, kit, quantity, user) {
      this.product = product;
      this.kit = kit;
      this.quantity = quantity;
      this.user = user;
    }
  }
